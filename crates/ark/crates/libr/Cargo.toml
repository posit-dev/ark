[package]
name = "libr"
version = "0.1.0"
edition = "2021"
rust-version = "1.75.0"
description = """
Bindings to R's C API, resolved dynamically at runtime.
"""

[dependencies]
cfg-if = "1.0.0"
libc = "0.2.152"
libloading = "0.8.1"
paste = "1.0.14"
